#!/usr/bin/env node

const utils = require('../utils');
const controller = require('../controller');

let code = process.argv.slice(2)[0]

code = utils.affiliationPad(code)

controller
	.queryStock(`s_${code}`)
    .then(ret => {
    	let str = ret.match(/\"(.*)\"/ig)[0];
	  	console.log(str)
	})

