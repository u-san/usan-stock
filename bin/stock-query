#!/usr/bin/env node

const utils = require('../utils');
const controller = require('../controller');

 console.log(process.argv)
 let code = process.argv.slice(2)[0]


 let a = utils.affiliationPad(code)
 console.log(a)
controller.queryStock(a)
          .then(ret => {
          	console.log(ret)
          })

